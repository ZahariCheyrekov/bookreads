name: Auto-fix npm security vulnerabilities (client)

on:
  schedule:
    - cron: "0 3 * * *" # daily at 3 AM UTC – feel free to change
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  audit-and-fix-client:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js for client
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: bookreads/client/package-lock.json

      - name: Install dependencies (client)
        working-directory: bookreads/client
        run: npm ci --legacy-peer-deps

      - name: Run npm audit fix (client)
        working-directory: bookreads/client
        run: npm audit fix --legacy-peer-deps
        continue-on-error: true

      - name: Create PR to main (if client changes)
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore(security): auto-fix npm vulnerabilities in client --legacy-peer-deps"
          title: "chore(security): npm audit fix updates (client)"
          body: |
            Automated `npm audit fix --legacy-peer-deps` in /bookreads/client.

            - Fixes applied where possible.
            - Review dependency changes and test the client before merging to main.
          branch: auto-security-fixes-client
          delete-branch: true
          base: main
          labels: dependencies, security, client
