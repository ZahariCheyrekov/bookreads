import { Link } from 'react-router-dom';

import Logo from '../Logo/Logo';

const Auth = () => {
    return (
        <main>
            <Logo />
            <form>
                <legend></legend>

                <label />
                <input />

                <label />
                <input />

                <button>

                </button>
            </form>
        </main>
    );
}

export default Auth;