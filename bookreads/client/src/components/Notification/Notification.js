import './Notification.css';

const Notification = () => {
    return (
        <section className="notification active">
            Notification;
        </section>
    );
}

export default Notification;